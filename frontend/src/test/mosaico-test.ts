// Test: Verificar se MosaicosPage compila
import { cameraService } from '../services/api';
import { useDynamicMosaic } from '../hooks/useDynamicMosaic';

console.log('OK: Imports funcionando');
console.log('OK: cameraService.getStream existe');
console.log('OK: useDynamicMosaic existe');

export {};
